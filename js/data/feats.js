// SRD 5.1 does not include feats. Users may add homebrew feats from their own sources.
// This module exposes the list shape so UI code can render an empty state gracefully.

export const FEATS = {};

export const FEAT_IDS = Object.keys(FEATS);
